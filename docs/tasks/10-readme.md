# 10 — README

## Scope

Root `README.md`:
- Project overview (what this is)
- Prerequisites (Node, pnpm, Docker)
- Setup and run instructions (`pnpm install`, `docker compose up`, seed, etc.)
- Running tests
- API documentation pointer (`/docs`)
- Short note on what a real deploy (Fly.io/Render for api, static host for web) would need beyond this scope

## Acceptance Criteria

- Clear setup instructions that work from a fresh clone
- Mentions all prerequisites
- Deploy notes acknowledge what's missing for production

## Dependencies

- 07 (Docker — need to reference docker-compose commands)
